// Social auth (Google, Apple, Facebook) will be enabled in the App Store build
// once OAuth client IDs are configured and the app is built with EAS.
// For now, all sign-in flows use the UUID-based guest identity from GameContext.
